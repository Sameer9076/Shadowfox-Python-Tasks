for = 4 # Python will throw the following error

# SyntaxError: invalid syntax
# Reason behind this behavior
​# Reserved Keyword: In Python, 
# for is a strictly reserved keyword used exclusively to define loops (like for i in range()).
​# Naming Restrictions: Python syntax does not allow keywords to be redefined or used,
# as identifier names (such as variable names, function names, or class names).
​# Syntax Violation: When the interpreter encounters for
# followed by an assignment operator (=), it recognizes it as a violation of Python's grammar rules,
#  resulting in a SyntaxError.
